        /* ---- HERO ---- */
   .hero {
      position: relative;
      height: 100vh;
      display: flex;
      align-items: center;
      justify-content: center;
      text-align: center;
      overflow: hidden;
    }

    /* ── Video – fills screen, no overlay ── */
    .hero-video {
      position: absolute;
      inset: 0;
      width: 100%;
      height: 100%;
      object-fit: cover;           /* or 'contain' if you prefer no cropping */
      object-position: center;
      z-index: 1;
      /* Very light adjustment – keeps colors vivid but text readable */
      filter: brightness(0.75) contrast(1.15) saturate(1.2);
    }

    /* ── Content on top ── */
    .hero-content {
      position: relative;
      z-index: 3;
      max-width: 900px;
      padding: 3rem 2rem;
      /* Optional: semi-transparent glass effect behind text only */
      background: rgba(0, 0, 0, 0.45);
      border-radius: 16px;
      backdrop-filter: blur(10px);
      -webkit-backdrop-filter: blur(10px);
      box-shadow: 0 10px 40px rgba(0,0,0,0.6);
    }

    h1 {
      font-size: clamp(3.5rem, 9vw, 7rem);
      font-weight: 800;
      line-height: 1;
      margin-bottom: 1.5rem;
      text-shadow: 0 4px 20px rgba(0,0,0,0.9);
      letter-spacing: -1px;
    }

    p {
      font-size: clamp(1.2rem, 4vw, 1.6rem);
      color: #e0e0ff;
      max-width: 680px;
      margin: 0 auto 2.5rem;
      text-shadow: 0 2px 12px rgba(0,0,0,0.8);
    }

    .btn {
      display: inline-block;
      padding: 1rem 2.5rem;
      background: linear-gradient(90deg, #7c3aed, #c084fc);
      color: white;
      font-weight: 600;
      font-size: 1.1rem;
      border-radius: 999px;
      text-decoration: none;
      transition: all 0.3s;
      box-shadow: 0 8px 30px rgba(124,58,237,0.35);
    }

    .btn:hover {
      transform: translateY(-3px);
      box-shadow: 0 16px 50px rgba(124,58,237,0.5);
    }

    @media (max-width: 640px) {
      .hero-content {
        padding: 2rem 1.5rem;
        margin: 0 1rem;
      }
      h1 { font-size: clamp(2.8rem, 10vw, 5rem); }
      p  { font-size: 1.15rem; }
    }
